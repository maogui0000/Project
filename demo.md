### 🌐 語言支援

* 中文

### 🐍 後端環境

* Python `3.14.5`（支援最新非同步異步處理 `asyncio`）

---
### 註冊介面
#### 1. 使用者合規與設定

* **使用者同意機制與資料保留政策說明** $\rightarrow$ 提示「AI 內容僅供參考」 $\rightarrow$ 引導長者/照護者進行注冊（姓名、疾病等基本資料） $\rightarrow$ 註冊完成後，以使用者姓名進行親切招呼（例如：「阿香婆婆，歡迎你！小黃以後會陪你聊天喔～」）。

### 🎙️ 語音聊天介面

#### 2. 喚醒與語音活動控制（VAD 優化）

* 🗣️ **喚醒詞**：「小黃小黃」 $\rightarrow$ 即刻開啟語音對話 Session。
* ⏱️ **連續對話機制**：引入 **VAD（語音活動偵測）**。喚醒後開啟對話視窗，長者停頓或思考時不切斷錄音；長者無需重複喊喚醒詞即可連續對話，直到說出「道別關鍵字」或靜音超過 30 秒才自動關閉 Session("說明一下適用哪個摩型進行")。

#### 3. 語音處理管線（串流串接優化）

* 📥 **ASR（語音轉文字）** $\rightarrow$ 串流（Streaming）輸入給聊天 Agent。
* 🎭 **語音情緒辨識（SER, Speech Emotion Recognition）**：與 ASR 並行對語音訊號進行情緒分類（如：開心、平靜、焦慮、悲傷、憤怒），辨識結果即時注入系統提示詞的環境變數中，供 LLM 動態調整回應語氣。
* 🧠 **聊天 Agent（LLM）** $\rightarrow$ 結合長短記憶與環境變數，以 Stream 方式一字字輸出。
* 📤 **TTS（文字轉語音）與文字顯示**：採用**分句即時合成技術**。LLM 吐出第一個句號或感嘆詞時，後端立刻丟給 TTS 播放並同步顯示文字，將語音體感延遲降低至 1 秒內，避免長者因無聲而感到困惑。

#### 4. 資料統整與記憶控制

* 記憶切分為 4 個獨立 JSON 模組（基本資料、長期記憶、短期快取、看板日誌）。
* 實施LLM判斷短期記憶的生命週期（TTL），過期自動清理，維持輕量化。

#### 5. 介面視覺

* 顯示系統專屬 Logo 圖示與動態語音波形（讓長者知道小黃正在聽）。

---

### 📝 提示詞（Prompt）核心邏輯

#### 1. 環境變數與脈絡融合

* 動態提取：當前時間（時段判斷） + 即時天氣 API + 長者個人基本資料中的「健康狀況（如：高血壓、糖尿病）」+ **當前語音情緒辨識結果（如：開心、焦慮）**，讓 LLM 根據長者即時情緒狀態調整回應策略與語氣。

#### 2. 記憶統整與觸發時機（異步優化）

* ⚠️ **觸發時機**：取消「每輪對話立刻分析」的舊做法。改為**當 Session 結束或對話閒置（Timeout）超過 2 分鐘時**，在背景非同步（Async）啟動一次性 Prompt 分析。
* **分析任務**：比對該場次短期對話，判斷是否有「新事件」（如：身體不適、提到了解子女、新的飲食喜好），若有則增量寫入長期記憶。

#### 3. 長短記憶控制

* 控制短期對話的快取時長（TTL），動態過濾無意義的贅詞，僅保留關鍵實體（Entities）。

---

### 📊 照護者看板 (Dashboard)

#### 1. 資料可視化介面

* 讀取結構化的 JSON 日誌，將長者的飲食、運動、用藥順從率（Compliance Rate）以及一週對話頻率趨勢轉化為直觀的圖表與時間軸。("情緒也要呈現")

#### 2. 今日摘要與推播

* 📅 **定時通知**：每天晚上 7:00 透過 Line Bot 將今日生活摘要推播給竹科等外地工作的子女。
* ⏳ **發送緩衝機制**：晚上 7:00 觸發時，定時任務會先檢查背景分析是否仍在執行。若長者剛好在 18:55 進行重要對話，系統將自動延遲 5 分鐘發送，確保 Line 通知包含最即時、完整的照護摘要，避免資訊漏接。

#### 3. 介面視覺

* 看板主視覺與 Line Bot 專屬 Logo 圖示。
