# ═══════════════════════════════════════════════════════
# 雲湧智生 - 智慧長照關懷系統 統一依賴清單
# ═══════════════════════════════════════════════════════

# ── Web 伺服器 (FastAPI + LINE Bot) ──────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
python-multipart==0.0.9
waitress==3.0.0
flask==3.0.3
line-bot-sdk==3.11.0

# ── AI 對話 (Amazon Bedrock) ─────────────────────────
boto3>=1.34.0
botocore>=1.34.0
requests==2.32.3

# ── 語音辨識 ASR (Taiwan-Tongues-ASR + Whisper) ──────
transformers>=4.40.0
torch>=2.2.0
torchaudio>=2.2.0
soundfile>=0.12.1
numpy==1.26.4
opencc-python-reimplemented==0.1.7

# ── 語音辨識 STT (備援本地 Whisper — EC2 部署可不裝) ──
# openai-whisper==20231117
SpeechRecognition==3.10.4

# ── 語音合成 TTS ─────────────────────────────────────
edge-tts>=6.1.9
pyttsx3==2.90
gTTS==2.5.1

# ── 喚醒詞偵測 (離線) ────────────────────────────────
vosk==0.3.45

# ── 音訊處理與播放 ───────────────────────────────────
pyaudio==0.2.14
sounddevice==0.4.7
miniaudio==1.60
playsound==1.2.2

# ── 天氣 API ─────────────────────────────────────────
urllib3==2.2.2

# ── 資料處理工具 ─────────────────────────────────────
python-dotenv==1.0.1

# ── 語音情緒辨識 (emotion2vec+) ──────────────────────
funasr>=1.3.0
modelscope>=1.38.0

# ── 模型訓練 (可選，僅開發時需要) ────────────────────
# datasets>=2.18.0
# huggingface_hub>=0.23.0
